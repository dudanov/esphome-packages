packages:
  firmware: !include
    file: template.yaml
    vars:
      id: ${id}
      signature_id: ${signature_id}
      sw_id: fw
      name: Firmware
      address: 250
      register_type: read
      response_size: 32

  bootloader: !include
    file: template.yaml
    vars:
      id: ${id}
      signature_id: ${signature_id}
      sw_id: bootloader
      name: Bootloader
      address: 330
      register_type: holding
      response_size: 14
