# Binary room light template

light:
  - platform: binary
    device_id: ${dev}_device
    name: None
    icon: mdi:ceiling-light
    output: output_pin${gpio}

output:
  - platform: gpio
    id: output_pin${gpio}
    pin: ${gpio}
