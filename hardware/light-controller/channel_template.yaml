# Binary room light template

light:
  - platform: binary
    name: ${room} Light
    icon: mdi:ceiling-light
    output: output_pin${gpio}

output:
  - platform: gpio
    id: output_pin${gpio}
    pin: ${gpio}
