# Binary room light template

light:
  - platform: binary
    id: ${id}_light
    device_id: ${id}_device
    name: None
    icon: mdi:ceiling-light
    output: pin${pin}_output

output:
  - platform: gpio
    id: pin${pin}_output
    pin: ${pin}
