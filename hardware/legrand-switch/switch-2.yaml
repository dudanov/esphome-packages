packages:
  # Main switch
  parent: !include switch-1.yaml
  # Adds two keys of the second switch
  key3: !include
    file: key_template.yaml
    vars:
      num: 3
      pin: 26
  key4: !include
    file: key_template.yaml
    vars:
      num: 4
      pin: 27
